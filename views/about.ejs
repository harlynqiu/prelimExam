<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>

<body>
    <% console.log(studentData) %>
    <h1>About Me</h1>
    <h2> by Harlyn Nichole Qiu</h2>
    <button><a href="/"> Click</a></button>
    <% for (let i = 0; i < studentData.length; i++){ %>
        <h1>
            <%= studentData[i].name %>
            <%= studentData[i].age %>
            <%= studentData[i].course %>
            <%= hellofunction(studentData[i].name) %>
        </h1>

    <% } %>

    <% studentData.forEach(function(student) { %>

        <h2>
            
        </h2>
        
    })
</body>
</html>